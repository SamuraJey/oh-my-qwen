# Extension template

`omq setup` generates the live Qwen extension from `src/qwen/extension-manifest.ts`.
This directory is packaged as a documented placeholder so future releases can move generated content to static templates without changing the npm file contract.
